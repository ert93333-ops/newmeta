# 사용자 확정 답변 반영본

## 1. 방향성

1-1. 제품은 단일 기능 도구가 아니라 모든 기능을 포함한 통합 플랫폼이다.

1-2. 1차 사용자는 사용자 본인이다. 즉 공개 SaaS보다 내부 자동화 도구 성격이 우선이다. 하지만 외부 고객 광고계정 연결도 필요하므로, 설계는 멀티테넌트/외부 계정 연결 기준으로 한다.

1-3. 사용자의 최우선 목표는 광고 운영 시간을 줄이는 것이다.

1-4. 차별점은 다음이다.

- 단순 리포트가 아니라 병목 원인을 설명한다.
- 이미지/영상의 구성요소를 해부한다.
- 소재 요소와 Meta 성과를 연결한다.
- #1487569 같은 placement 오류를 사전 방지한다.
- 안전영역/텍스트/가격 정확도를 자동 검수한다.
- 승인 기반으로만 광고를 실행한다.

## 2. MVP 범위

2-1. MVP는 별도로 축소하지 않는다. 모든 기능이 최종 구현 범위다.

2-2. 예산 자동 변경은 제외한다.

2-3. 가능한 모든 광고 형식을 지원한다.

2-4. 가능한 모든 placement를 지원한다.

## 3. Meta 연결

3-1. 권장 구조를 따른다: MetaAdapter Interface + MetaGraphApiAdapter + MetaMcpAdapter + MockMetaAdapter.

3-2. 외부 고객의 광고계정 연결이 필요하다.

3-3. 필요한 권한은 전부 지원한다.

3-4. 예산 변경을 제외한 모든 실행 기능은 선택 가능하게 지원한다. 단, 위험 액션은 승인 기반이다.

## 4. Creative Analysis

4-1. 이미지 분석 항목은 전부 지원한다.

4-2. 영상 분석 항목은 전부 지원한다.

4-3. hook type은 전부 지원한다.

4-4. 제품 인지 분석은 조사 후 가장 적절한 방법으로 구현한다.

4-5. 텍스트 정확도는 권장 설정으로 구현한다.

## 5. Bottleneck Diagnosis

5-1. 퍼널 단계는 전부 진단한다.

5-2. Meta 데이터, Pixel, GA4, 자사몰 데이터를 모두 연동 가능하게 한다. Shopify는 필수 지원하지 않는다.

5-3. 이벤트 매핑은 권장 방식으로 구현한다.

5-4. 일예산 5만원 기준에 맞춰 데이터 충분성 기준을 조정한다.

5-5. 기준값/benchmark는 조사 후 권장 방법으로 구현한다.

5-6. 병목 점수 계산은 조사 후 권장 방법으로 구현한다.

## 6. Performance Fusion

6-1, 6-2, 6-3. 소재 요소와 성과 연결, 인과관계 제한, 신뢰도 표시는 조사 후 권장 방식으로 구현한다.

## 7. Placement Validator

7-1, 7-2. #1487569 방지와 placement별 권장 소재는 조사 후 권장 방식으로 구현한다.

## 8. Creative Renderer

8-2. 지원 사이즈는 전부 지원한다.

## 10. Draft Creator / Approval

10-1. 예산 변경을 제외한 실행 기능은 전부 지원한다. 단, 승인/권한 레벨로 통제한다.

## 11. UI

11-4. 경고 UI는 한글로 알기 쉬운 용어로 표시한다.

## 12. 데이터

12-1. tenant_id를 처음부터 넣는다.

12-2. 필요한 데이터는 전부 저장한다.

12-3. 데이터는 사용자가 삭제할 때까지 보관한다.

12-4. 알고리즘 개선에는 합쳐서 활용하되, 원본 고객 데이터 혼합은 금지한다. 익명화/집계/옵트인된 패턴 데이터만 cross-tenant learning에 사용한다.

## 14. 운영

14-1. Supabase를 중심으로 구축한다.

14-3. Higgsfield 기준으로 손해가 나지 않는 방향의 비용 제한 정책을 구현한다.

## 15. 테스트

15-1. 모든 테스트를 구현한다.

## 16. 정책

16-1. 허위 가격, 금지 상품, 의료, 금융, 성인, 위험 상품에 대한 정책 위험 검사를 구현한다.

16-2. AI는 잘못된 정보나 없는 정보를 꾸며내면 안 된다.

## 미답변 항목 처리

사용자가 따로 답하지 않은 항목은 전부 조사 후 권장 설정으로 확정한다.
